<Page
    x:Class="MediaPerch.Shell.Pages.PlaylistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,Auto,*" Padding="24,8,24,16" RowSpacing="8">
        <TextBlock x:Name="Heading" Grid.Row="0" Text="Playlist"
                   Style="{StaticResource TitleTextBlockStyle}" />
        <InfoBar x:Name="Note" Grid.Row="1" IsOpen="False" Severity="Warning" />

        <!-- A click plays that track: the run starts again there, which is a
             real gap and is what a person who clicked asked for. The arrow
             marks what is being heard, not what the decoder is on. -->
        <ListView x:Name="Rows" Grid.Row="2"
                  IsItemClickEnabled="True"
                  SelectionMode="None"
                  ItemClick="OnItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis"
                               Style="{StaticResource BodyTextBlockStyle}"
                               Padding="0,6" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
