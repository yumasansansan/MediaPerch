<Page
    x:Class="MediaPerch.Shell.Pages.NowPlayingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <Grid RowDefinitions="*,Auto">

        <!-- The picture, and nothing else in its way. §9.7.1: the engine draws
             into a composition surface it never shows, the handle crosses once,
             and this is where it becomes something the window composites. The
             host is transparent on purpose: the picture is the box this page
             asks the engine for, placed and centred by SurfaceHost, and around
             it is the window's own acrylic rather than a letterbox. -->
        <Border x:Name="Picture" Grid.Row="0"
                Margin="16,8,16,0"
                SizeChanged="OnPictureResized" />
        <TextBlock x:Name="PictureLine" Grid.Row="0"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Style="{StaticResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" TextAlignment="Center" MaxWidth="480" />

        <!-- The transport, in the shape every player's takes: what is playing,
             where in it, and the buttons. Only the verbs §10 has. -->
        <Grid Grid.Row="1" Padding="24,12,24,16" RowSpacing="6"
              RowDefinitions="Auto,Auto,Auto">

            <StackPanel Grid.Row="0" Spacing="2">
                <TextBlock x:Name="TrackLine"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           Text="Nothing is playing"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="FormatLine"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>

            <Grid Grid.Row="1" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                <TextBlock x:Name="PositionNow" Grid.Column="0" MinWidth="40"
                           VerticalAlignment="Center" TextAlignment="Right"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <!-- Into this track, against this track's length: the two
                     numbers status answers in the same unit. A drag seeks to the
                     queue's coordinate, which is where this track began plus the
                     value, because that is what the engine's seek speaks. -->
                <Slider x:Name="Scrub" Grid.Column="1" VerticalAlignment="Center"
                        IsThumbToolTipEnabled="False" IsEnabled="False"
                        ValueChanged="OnScrubChanged" />
                <TextBlock x:Name="PositionEnd" Grid.Column="2" MinWidth="40"
                           VerticalAlignment="Center"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8"
                        HorizontalAlignment="Center">
                <Button Click="OnOpen" Width="40" Height="40" Padding="0" Margin="0,0,16,0"
                        ToolTipService.ToolTip="Open files">
                    <FontIcon Glyph="&#xE8E5;" FontSize="16" />
                </Button>
                <Button Click="OnPrevious" Width="40" Height="40" Padding="0"
                        ToolTipService.ToolTip="Previous">
                    <FontIcon Glyph="&#xE892;" FontSize="16" />
                </Button>
                <Button Click="OnBackTen" Width="40" Height="40" Padding="0"
                        ToolTipService.ToolTip="Back ten seconds">
                    <FontIcon Glyph="&#xED3C;" FontSize="16" />
                </Button>
                <Button x:Name="PlayPause" Click="OnPlayPause"
                        Style="{StaticResource AccentButtonStyle}"
                        Width="56" Height="56" Padding="0" CornerRadius="28"
                        ToolTipService.ToolTip="Play or pause">
                    <FontIcon x:Name="PlayGlyph" Glyph="&#xE768;" FontSize="22" />
                </Button>
                <Button Click="OnForwardTen" Width="40" Height="40" Padding="0"
                        ToolTipService.ToolTip="Forward ten seconds">
                    <FontIcon Glyph="&#xED3D;" FontSize="16" />
                </Button>
                <Button Click="OnNext" Width="40" Height="40" Padding="0"
                        ToolTipService.ToolTip="Next">
                    <FontIcon Glyph="&#xE893;" FontSize="16" />
                </Button>
                <Button Click="OnStop" Width="40" Height="40" Padding="0" Margin="16,0,0,0"
                        ToolTipService.ToolTip="Stop">
                    <FontIcon Glyph="&#xE71A;" FontSize="16" />
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
